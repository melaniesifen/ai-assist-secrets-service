[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-assist-secrets-service"
version = "0.1.0"
description = "Domain-layer Python package for short-lived provider API key SessionSecrets."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
