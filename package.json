{
  "name": "ai-assist-secrets-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Domain-layer bootstrap for short-lived AI provider SessionSecrets.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
